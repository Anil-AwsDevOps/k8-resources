apiVersion: v1 
kind: Pod 
metadata:
  name: resoucre
  spec:
  containers: 
  - name: nginx
    image: nginx
    resources:
    #soft limit 
      requests:
        memory: "64mi" #RAM
        cpu: "100m" #cpu
      # hard limit
      limits:
        memory: "128mi"
        cpu: "120m"
      
    