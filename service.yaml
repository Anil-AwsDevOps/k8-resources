apiVersion: v1
kind: Service
metadata:
  name: myapp
spec:
  selector:
    # labels are used as selectors
    project: roboshop 
    component: frontend
    environment: dev
  ports:
  - port: 80 # service port 
    targetPort: 80 # container port 
